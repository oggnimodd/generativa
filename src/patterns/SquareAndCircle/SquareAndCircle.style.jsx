import tw, { styled } from 'twin.macro';

export const Circle = styled.div`
  ${tw`
    w-full 
    h-full 
    rounded-full 
    flex 
    flex-wrap
  `}
`;

export const Triangle = styled.div`
  ${tw`
    absolute  
  `}
`;
